{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "desktop",
  "description": "Desktop-only capabilities. Kept out of `default` because the plugins behind them are not in the mobile dependency set at all — an unknown permission is a build error there, not a no-op.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main", "session-*", "instance-*", "tile-*", "sat-*"],
  "permissions": [
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "global-shortcut:allow-unregister-all",
    "global-shortcut:allow-is-registered"
  ]
}
